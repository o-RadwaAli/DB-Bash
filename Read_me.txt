You can .....
--------------
1) Create DB and populate it with tables' definitions (Datatypes can be either number, string or mix). 
2) Use DB 
	2.a) Create table 
	2.b) Delete table (if table in fact exists)
	2.c) Display table (select * from tbl)
	2.d) Insert Rows (with validations on PK constraint and data types)
	2.e) Delete row by ID (with validations on ID existance)
	2.f) Update row 
	2.g) Desc table 
3) Delete a DB.
4) List all created DBs.